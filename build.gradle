allprojects  {
  apply plugin: 'maven'

  group = 'com.unquietcode.tools.jcodemodel'
  version = '1.0.4'
}

subprojects {
  repositories {
    mavenCentral()
  }
}

task wrapper(type: Wrapper) {
	gradleVersion = '2.3'
}